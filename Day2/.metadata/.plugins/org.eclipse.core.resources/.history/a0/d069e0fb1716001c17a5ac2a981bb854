package com.exercise.demo;

public class ItemService {
	   public double findDiscount(Item item) {
		   double discount= (item.getRatePerUnit() * 0.05);
		   return discount;
	   }
	   
	   public double findDiscount(Item item,String customer) {
		   double discount=(item.getRatePerUnit() * 0.10);
		   String typ
		   if(customer.equals("corporate")) {
			   discount=(item.getRatePerUnit() * 0.15);
		   }
		   return discount;
	   }
}
